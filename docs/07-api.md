# Interfaces HTTP y API

Última revisión documental: 2026-09-10. Estado: no se encontró API propia.

Astro genera la ruta de página raíz a partir de `src/pages/index.astro`. No se encontraron `src/pages/api`, endpoints server-side, GraphQL, WebSocket, webhook ni OpenAPI. El enlace `mailto:` no constituye una API del repositorio.
